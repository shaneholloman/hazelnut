---
import Layout from '../layouts/Layout.astro';

const baseUrl = import.meta.env.BASE_URL;
---

<Layout title="Hazelnut - Terminal File Organizer">
  <section class="hero">
    <div class="hero-content">
      <h1>
        <span class="logo-emoji">🌰</span>
        Hazelnut
      </h1>
      <p class="hero-tagline">
        A beautiful terminal-based file organizer<br />
        <span class="highlight">inspired by Hazel</span>
      </p>
      <p class="hero-description">
        Watch folders and automatically organize files with flexible rules —
        all from your terminal with a gorgeous TUI.
      </p>

      <div class="install-box">
        <div class="install-tabs">
          <button class="install-tab active" data-tab="homebrew">Homebrew</button>
          <button class="install-tab" data-tab="cargo">Cargo</button>
        </div>
        <div class="install-commands">
          <div class="install-command active" id="homebrew-cmd">
            <code>brew install ricardodantas/tap/hazelnut</code>
            <button class="copy-btn" data-copy="brew install ricardodantas/tap/hazelnut" aria-label="Copy to clipboard">
              <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2">
                <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
                <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
              </svg>
              <span class="copy-text">Copy</span>
            </button>
          </div>
          <div class="install-command" id="cargo-cmd">
            <code>cargo install hazelnut-cli</code>
            <button class="copy-btn" data-copy="cargo install hazelnut-cli" aria-label="Copy to clipboard">
              <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2">
                <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
                <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
              </svg>
              <span class="copy-text">Copy</span>
            </button>
          </div>
        </div>
      </div>

      <div class="hero-buttons">
        <a href={`${baseUrl}docs`} class="btn btn-primary">
          Get Started
          <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>
        <a href="https://github.com/ricardodantas/hazelnut" target="_blank" rel="noopener" class="btn btn-secondary">
          <svg viewBox="0 0 24 24" width="18" height="18" fill="currentColor">
            <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
          </svg>
          View on GitHub
        </a>
      </div>
    </div>

    <div class="hero-demo">
      <div class="demo-window">
        <div class="demo-titlebar">
          <span class="dot red"></span>
          <span class="dot yellow"></span>
          <span class="dot green"></span>
          <span class="demo-title">hazelnut</span>
        </div>
        <div class="demo-content">
          <img src={`${baseUrl}screenshots/01-dashboard.png`} alt="Hazelnut TUI dashboard" loading="lazy" />
        </div>
      </div>
    </div>
  </section>

  <section class="features" id="features">
    <div class="container">
      <h2>Features</h2>
      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon">📁</div>
          <h3>Watch Folders</h3>
          <p>Monitor directories for new and changed files in real-time. Set up multiple watch locations with different rules.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">🎯</div>
          <h3>Flexible Rules</h3>
          <p>Create powerful rules with conditions based on file name, type, size, age, and more using glob patterns or regex.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">⚡</div>
          <h3>Smart Actions</h3>
          <p>Move, copy, rename, archive, delete, or run custom scripts on matched files. Chain actions together.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">🖥️</div>
          <h3>Beautiful TUI</h3>
          <p>Manage rules and monitor activity through an intuitive terminal interface with 15 gorgeous themes.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">🔧</div>
          <h3>Background Daemon</h3>
          <p>The daemon runs quietly in the background, processing files automatically as they appear.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">📝</div>
          <h3>TOML Config</h3>
          <p>Human-readable TOML configuration that's easy to version control and share with your team.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="examples">
    <div class="container">
      <h2>Example Rules</h2>
      <div class="examples-grid">
        <div class="example-card">
          <h3>📸 Organize Screenshots</h3>
          <pre><code>[[rule]]
name = "Screenshots"

[rule.condition]
name_matches = "Screenshot*.png"

[rule.action]
type = "move"
destination = "~/Pictures/Screenshots"</code></pre>
        </div>

        <div class="example-card">
          <h3>🗑️ Clean Old Downloads</h3>
          <pre><code>[[rule]]
name = "Clean old downloads"

[rule.condition]
age_days_greater_than = 30

[rule.action]
type = "trash"</code></pre>
        </div>

        <div class="example-card">
          <h3>📄 Sort PDFs</h3>
          <pre><code>[[rule]]
name = "Move PDFs"

[rule.condition]
extension = "pdf"

[rule.action]
type = "move"
destination = "~/Documents/PDFs"</code></pre>
        </div>
      </div>

      <div class="examples-cta">
        <a href={`${baseUrl}docs`} class="btn btn-primary">
          See More Examples
          <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>
      </div>
    </div>
  </section>

  <section class="themes-preview">
    <div class="container">
      <h2>15 Beautiful Themes</h2>
      <p class="section-description">
        Customize your experience with gorgeous color schemes including Dracula, Nord, Tokyo Night, and more.
      </p>
      <div class="themes-showcase">
        <img src={`${baseUrl}screenshots/02-theme-picker.png`} alt="Theme picker showing available themes" class="theme-screenshot" loading="lazy" />
      </div>
      <div class="themes-cta">
        <a href={`${baseUrl}themes`} class="btn btn-secondary">
          Browse All Themes
          <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>
      </div>
    </div>
  </section>

  <section class="quick-start">
    <div class="container">
      <h2>Quick Start</h2>
      <div class="steps">
        <div class="step">
          <div class="step-number">1</div>
          <div class="step-content">
            <h3>Install</h3>
            <div class="step-code">
              <code>brew install ricardodantas/tap/hazelnut</code>
              <button class="copy-btn small" data-copy="brew install ricardodantas/tap/hazelnut" aria-label="Copy">
                <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2">
                  <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
                  <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
                </svg>
              </button>
            </div>
            <p class="step-alt">or <code>cargo install hazelnut-cli</code></p>
          </div>
        </div>

        <div class="step">
          <div class="step-number">2</div>
          <div class="step-content">
            <h3>Start the Daemon</h3>
            <div class="step-code">
              <code>hazelnutd start</code>
              <button class="copy-btn small" data-copy="hazelnutd start" aria-label="Copy">
                <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2">
                  <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
                  <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
                </svg>
              </button>
            </div>
          </div>
        </div>

        <div class="step">
          <div class="step-number">3</div>
          <div class="step-content">
            <h3>Launch the TUI</h3>
            <div class="step-code">
              <code>hazelnut</code>
              <button class="copy-btn small" data-copy="hazelnut" aria-label="Copy">
                <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2">
                  <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
                  <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
                </svg>
              </button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* Hero Section */
  .hero {
    padding: calc(var(--nav-height) + 4rem) 1.5rem 4rem;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 3rem;
    background: linear-gradient(180deg, var(--bg-secondary) 0%, var(--bg-primary) 100%);
  }

  .hero-content {
    text-align: center;
    max-width: 700px;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: var(--bg-tertiary);
    padding: 0.5rem 1rem;
    border-radius: 999px;
    font-size: 0.875rem;
    margin-bottom: 1.5rem;
    color: var(--accent-orange);
  }

  h1 {
    font-size: clamp(3rem, 8vw, 5rem);
    font-weight: 800;
    margin-bottom: 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
  }

  .logo-emoji {
    font-size: 0.8em;
  }

  .hero-tagline {
    font-size: clamp(1.25rem, 3vw, 1.5rem);
    color: var(--text-secondary);
    margin-bottom: 1rem;
    line-height: 1.4;
  }

  .highlight {
    color: var(--accent-purple);
  }

  .hero-description {
    font-size: 1.125rem;
    color: var(--text-secondary);
    margin-bottom: 2rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .install-box {
    display: inline-flex;
    flex-direction: column;
    align-items: stretch;
    background: var(--bg-secondary);
    border: 1px solid var(--bg-tertiary);
    border-radius: 8px;
    margin-bottom: 2rem;
    font-family: var(--font-mono);
    min-width: 320px;
  }

  .install-tabs {
    display: flex;
    border-bottom: 1px solid var(--bg-tertiary);
  }

  .install-tab {
    flex: 1;
    padding: 0.75rem 1rem;
    background: transparent;
    border: none;
    color: var(--text-secondary);
    font-family: var(--font-sans);
    font-size: 0.875rem;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.2s;
  }

  .install-tab:first-child {
    border-radius: 8px 0 0 0;
  }

  .install-tab:last-child {
    border-radius: 0 8px 0 0;
  }

  .install-tab:hover {
    color: var(--text-primary);
    background: var(--bg-tertiary);
  }

  .install-tab.active {
    color: var(--accent-purple);
    background: var(--bg-primary);
    border-bottom: 2px solid var(--accent-purple);
    margin-bottom: -1px;
  }

  .install-commands {
    padding: 0.75rem 1rem;
  }

  .install-command {
    display: none;
    align-items: center;
    gap: 0.5rem;
  }

  .install-command.active {
    display: flex;
  }

  .install-box code {
    background: none;
    padding: 0;
    color: var(--accent-green);
    font-size: 1rem;
  }

  .copy-btn {
    display: flex;
    align-items: center;
    gap: 0.25rem;
    background: var(--bg-tertiary);
    border: none;
    color: var(--text-secondary);
    padding: 0.5rem 0.75rem;
    border-radius: 4px;
    cursor: pointer;
    transition: all 0.2s;
    font-family: var(--font-sans);
    font-size: 0.875rem;
  }

  .copy-btn:hover {
    background: var(--accent-purple);
    color: var(--bg-primary);
  }

  .copy-btn.copied {
    background: var(--accent-green);
    color: var(--bg-primary);
  }

  .copy-btn.small {
    padding: 0.25rem 0.5rem;
  }

  .copy-btn.small .copy-text {
    display: none;
  }

  .hero-buttons {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1rem;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.875rem 1.5rem;
    border-radius: 8px;
    font-weight: 600;
    font-size: 1rem;
    transition: all 0.2s;
    text-decoration: none;
  }

  .btn-primary {
    background: var(--accent-purple);
    color: var(--bg-primary);
  }

  .btn-primary:hover {
    background: var(--accent-pink);
    color: var(--bg-primary);
    transform: translateY(-2px);
  }

  .btn-secondary {
    background: var(--bg-tertiary);
    color: var(--text-primary);
  }

  .btn-secondary:hover {
    background: var(--text-secondary);
    color: var(--bg-primary);
    transform: translateY(-2px);
  }

  /* Demo Window */
  .hero-demo {
    width: 100%;
    max-width: 900px;
  }

  .demo-window {
    background: var(--bg-secondary);
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
    border: 1px solid var(--bg-tertiary);
  }

  .demo-titlebar {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 12px 16px;
    background: var(--bg-tertiary);
  }

  .dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
  }

  .dot.red { background: #ff5f56; }
  .dot.yellow { background: #ffbd2e; }
  .dot.green { background: #27c93f; }

  .demo-title {
    margin-left: auto;
    margin-right: auto;
    font-size: 0.875rem;
    color: var(--text-secondary);
    font-family: var(--font-mono);
  }

  .demo-content img {
    width: 100%;
    display: block;
  }

  /* Features Section */
  .features {
    padding: 6rem 1.5rem;
    background: var(--bg-secondary);
  }

  .container {
    max-width: var(--max-width);
    margin: 0 auto;
  }

  h2 {
    font-size: clamp(2rem, 5vw, 2.5rem);
    text-align: center;
    margin-bottom: 1rem;
  }

  .section-description {
    text-align: center;
    color: var(--text-secondary);
    font-size: 1.125rem;
    max-width: 600px;
    margin: 0 auto 3rem;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 1.5rem;
    margin-top: 3rem;
  }

  .feature-card {
    background: var(--bg-primary);
    padding: 2rem;
    border-radius: 12px;
    border: 1px solid var(--bg-tertiary);
    transition: transform 0.2s, border-color 0.2s;
  }

  .feature-card:hover {
    transform: translateY(-4px);
    border-color: var(--accent-purple);
  }

  .feature-icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
  }

  .feature-card h3 {
    font-size: 1.25rem;
    margin-bottom: 0.75rem;
    color: var(--text-primary);
  }

  .feature-card p {
    color: var(--text-secondary);
    line-height: 1.7;
  }

  /* Examples Section */
  .examples {
    padding: 6rem 1.5rem;
  }

  .examples-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 1.5rem;
    margin-top: 3rem;
  }

  .example-card {
    background: var(--bg-secondary);
    border-radius: 12px;
    overflow: hidden;
    border: 1px solid var(--bg-tertiary);
  }

  .example-card h3 {
    padding: 1rem 1.5rem;
    background: var(--bg-tertiary);
    font-size: 1rem;
    border-bottom: 1px solid var(--bg-tertiary);
  }

  .example-card pre {
    margin: 0;
    border: none;
    border-radius: 0;
    font-size: 0.8125rem;
  }

  .examples-cta {
    text-align: center;
    margin-top: 3rem;
  }

  /* Themes Preview */
  .themes-preview {
    padding: 6rem 1.5rem;
    background: var(--bg-secondary);
  }

  .themes-showcase {
    margin-top: 3rem;
    text-align: center;
  }

  .theme-screenshot {
    max-width: 100%;
    border-radius: 12px;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
    border: 1px solid var(--bg-tertiary);
  }

  .themes-cta {
    text-align: center;
    margin-top: 2rem;
  }

  /* Quick Start */
  .quick-start {
    padding: 6rem 1.5rem;
  }

  .steps {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    max-width: 500px;
    margin: 3rem auto 0;
  }

  .step {
    display: flex;
    gap: 1.5rem;
    align-items: flex-start;
  }

  .step-number {
    width: 48px;
    height: 48px;
    background: var(--accent-purple);
    color: var(--bg-primary);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 1.25rem;
    flex-shrink: 0;
  }

  .step-content h3 {
    font-size: 1.25rem;
    margin-bottom: 0.75rem;
  }

  .step-code {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    background: var(--bg-secondary);
    padding: 0.75rem 1rem;
    border-radius: 8px;
    border: 1px solid var(--bg-tertiary);
    font-family: var(--font-mono);
  }

  .step-code code {
    background: none;
    padding: 0;
    color: var(--accent-green);
  }

  .step-alt {
    margin-top: 0.5rem;
    font-size: 0.875rem;
    color: var(--text-secondary);
  }

  .step-alt code {
    font-size: 0.8125rem;
    padding: 0.125rem 0.375rem;
  }

  @media (max-width: 768px) {
    .hero {
      padding: calc(var(--nav-height) + 2rem) 1rem 2rem;
      gap: 2rem;
    }

    .hero-content {
      max-width: 100%;
    }

    h1 {
      font-size: 2.5rem;
      flex-wrap: wrap;
    }

    .hero-tagline {
      font-size: 1.125rem;
    }

    .hero-description {
      font-size: 1rem;
    }

    .install-box {
      width: 100%;
      max-width: 100%;
      min-width: unset;
    }

    .install-command {
      flex-wrap: wrap;
      justify-content: center;
      gap: 0.5rem;
    }

    .install-box code {
      font-size: 0.875rem;
      word-break: break-all;
    }

    .hero-buttons {
      flex-direction: column;
      width: 100%;
      gap: 0.75rem;
    }

    .btn {
      justify-content: center;
      width: 100%;
    }

    .hero-demo {
      width: 100%;
      max-width: 100%;
    }

    .demo-window {
      max-width: 100%;
    }

    .demo-content img {
      max-width: 100%;
      height: auto;
    }

    .features, .examples, .themes-preview, .quick-start {
      padding: 3rem 1rem;
    }

    .features-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .step {
      flex-direction: column;
      text-align: center;
      align-items: center;
    }

    .step-code {
      justify-content: center;
      flex-wrap: wrap;
    }

    .step-code code {
      font-size: 0.8rem;
      word-break: break-all;
    }

    .example-block {
      padding: 1rem;
    }

    .example-block pre {
      font-size: 0.75rem;
      overflow-x: auto;
    }

    .theme-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 0.75rem;
    }

    .theme-card {
      padding: 0.75rem;
    }

    .theme-colors {
      gap: 0.25rem;
    }

    .theme-color {
      width: 16px;
      height: 16px;
    }
  }

  @media (max-width: 480px) {
    h1 {
      font-size: 2rem;
    }

    .hero-badge {
      font-size: 0.75rem;
      padding: 0.375rem 0.75rem;
    }

    .install-box code {
      font-size: 0.75rem;
    }

    .copy-btn {
      padding: 0.375rem 0.5rem;
    }

    .copy-text {
      display: none;
    }

    .theme-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  // Install tab switching
  document.querySelectorAll('.install-tab').forEach(tab => {
    tab.addEventListener('click', () => {
      const targetTab = tab.getAttribute('data-tab');

      // Update tabs
      document.querySelectorAll('.install-tab').forEach(t => t.classList.remove('active'));
      tab.classList.add('active');

      // Update commands
      document.querySelectorAll('.install-command').forEach(cmd => cmd.classList.remove('active'));
      document.getElementById(`${targetTab}-cmd`)?.classList.add('active');
    });
  });

  // Copy to clipboard functionality
  document.querySelectorAll('.copy-btn').forEach(btn => {
    btn.addEventListener('click', async () => {
      const text = btn.getAttribute('data-copy');
      if (!text) return;

      try {
        await navigator.clipboard.writeText(text);
        btn.classList.add('copied');
        const textEl = btn.querySelector('.copy-text');
        if (textEl) textEl.textContent = 'Copied!';

        setTimeout(() => {
          btn.classList.remove('copied');
          if (textEl) textEl.textContent = 'Copy';
        }, 2000);
      } catch (err) {
        console.error('Failed to copy:', err);
      }
    });
  });
</script>
